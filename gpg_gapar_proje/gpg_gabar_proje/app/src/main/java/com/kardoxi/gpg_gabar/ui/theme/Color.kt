package com.kardoxi.gpg_gabar.ui.theme

import androidx.compose.ui.graphics.Color

// Professional, subdued Material 3-inspired palette
// Keep variable names stable to avoid changing call sites
val NeonCyan = Color(0xFF00BCD4)       // Teal 500
val NeonMagenta = Color(0xFFE91E63)    // Pink 600
val ElectricPurple = Color(0xFF6750A4) // M3 baseline primary
val LazerLime = Color(0xFF8BC34A)      // Light green 500

// Neutral surfaces (light/dark)
val DeepSpace900 = Color(0xFF121212)
val DeepSpace800 = Color(0xFF1E1E1E)
val DeepSpace700 = Color(0xFF2A2A2A)

// Containers aligned with brand accents
val CyanContainer = Color(0xFFB2EBF2)      // Teal 100
val MagentaContainer = Color(0xFFF8BBD0)   // Pink 100
val PurpleContainer = Color(0xFFEADDFF)    // M3 primary container light
val LimeContainer = Color(0xFFDCEDC8)      // Light green 100

// On-colors (content on top of containers)
val OnCyan = Color(0xFF004F58)
val OnMagenta = Color(0xFF5D1033)
val OnPurple = Color(0xFF21005D)           // M3 onPrimaryContainer
val OnLime = Color(0xFF254A16)
